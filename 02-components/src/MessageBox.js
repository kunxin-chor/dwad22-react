// 1.  a component must have the first alphabet of its function name
// as upper case
//
// 2. must return JSX
// 3. must be a function
export default function MessageBox() {
  return <div style={{"border":"1px solid black"}}>
    Take this! It's dangerous to go alone.
  </div>
}